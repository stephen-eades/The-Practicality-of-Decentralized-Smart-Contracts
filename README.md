# The Practicality of Decentralized Smart Contracts
IT8010 Capstone Project - Instructed by Dr. Murat Ozer

Stephen Eades - Eadessn@mail.uc.edu

## Synopsis
The growth of blockchain technology has facilitated the discussion of decentralized systems going mainstream. Everyone seems to want to apply blockchain technology to their usecase. Insurance, lending, and even video games and collectibles are being built on top of decentralized systems. One of the most popular and mature decentralized platforms is Ethereum, which allows developers to write determinisitic smart contracts that execute trustlessly by way of the Ethereum Virtual Machine. This project evaluates the use of decentralized computing and smart contracts built on Ethereum. I examine a decentralized application (dapp) and the transaction costs and feasability of utilizing such a dapp for common events such as a poll, escrow service, or lottery. Our data analysis shows that although these events can be processed successfully and securly, transaction costs are far too high for a regular user to interact with them effectively. Transaction information was pulled from Ganache and Truffle, two tools for working with Ethereum, and analyzed using Pandas and matplotlib within a Jupyter Notebook. Comparisons supported that several factors influence transaction costs, mainly the price flucuation of Ethereum's native token (Ether), and the amount of data being provided as input to the transactions. This additionally provided insight on the challenges and limitations of current decentralized applications, such that the benefits may not yet outweigh the costs, and may not until proper scaling is implemented on Ethereum. 

## Research Questions
* What are the associated transaction costs of interacting with a decentralized application on Ethereum?
* What factors influence the transaction costs on a decentralized application?
* Can a decentralized application effectively handle everyday events that require trust?

## Problem Statement
Blockchain technology has shown that the removal of trust in a system is possible, and many are wondering how this new technology will fit into a world where trust seems more important that ever. Smart contracts and decentralized applications on the Ethereum Blockchain have been a major point of focus in the newfound industry, and developers have begun to explore what this technology is capable of. However, there has yet to be major usage of decentralized applications by the general population. One concern is that the current costs associated with making transactions on a blockchain can be exorbantly high, as scaling and protocol solutions are still being implemented. These high costs likely drive potential users away from decentralized systems. Another issue is that a majority of current dapps are for financial services that a regular person wouldn't often use. Decentralized exchanges (DEXs) and decentralized lending and money markets, also known as Decentralized Finance (Defi), dominate the current dapp ecosystem. Most users of these dapps are wealthy indivudals and institutional investors looking for liquidity, arbitrage, and investment opportunities. 

Blockchain technology could help remove the middleman, and trust, from many situations where it would benefit the end users. Imagine a smart contract that automatically calculates and allocates your retirement funds for you based on your risk assessment, market conditions, and preferences, ultimately removing the need for a financial advisor. A decentralized version of many current platforms (Facebook, Uber, etc.) would remove the need for centralized servers and storing user data, which means companies would have an easier time with regulations like GDPR. Elections could be held entirely on a blockchain, allowing any user to check and validate any other vote that occurred, ensuring a election process that can't be tampered with or manipulated. These ideas all sound wonderful on paper, but how effective and practical they are for the user is yet to be determined. With the afforementioned concerns stated above, one might assume that these kind of decentralized applications and products are still years out from being ready for everyday usage by the general population. 

The dapp that I developed provides the user the ability to create and deploy their own smart contract event. The user simply inputs the event details, and the deployment process implements their details into the event logic. Similiar studies tended to evaluate a specific transaction and how it varied with time and the price of Ether, while my application will additionally explore and compare multiple transaction types (transfering Ether, deploying contracts, writing to contract storage, etc.) and how the user's input for the creation of each contract impacts the cost. I also believe the ability to let the user customize their own smart contract could be a way to help grow the adoption of decentralized applications for day-to-day usage. I hope my project can give future users, researchers, and developers awareness on the benefits and downsides of using and building a decentralized application. If successful, blockchain technology and decentralization could revolutionize how we exchange value and automate processes that require trust.

## Architecture & Engineering
When considering the architecture of this project, I first needed to choose the blockchain that I would build my application on. Out of the contenders, Ethereum was the most mature in both age and ecosystem. Ethereum came out in 2015, and many developers have created tutorials, written books, built tools and created applications that helped me come to the decision of building on Ethereum. Solidity is the language used for the Ethereum Virtual Machine (EVM) and will be what I write my smart contracts in. Truffle will be an important tool, as it helps with building, deploying, and testing the application. Another importance piece is having a testnet blockchain to work on before deploying to production. Ganache will come into play there, and provides us some accounts with fake ethereum to cover our transaction costs. The final part will be creating a frontend interface for users to interact with our smart contracts. Truffle happens to have some frameworks, such as Drizzle, that already has React components for the UI. This does a lot of the work for me on the frontend so I can focus on the smart contracts. Finally, Web3.js api will be used to connect the frontend with the Ethereum network, and will help us check if the user has the Metamask wallet extension. Without Metamask, the user won't be able to use their Ethereum to interact with the smart contracts. 

All-in-all, the tools, frameworks, and technologies I'm using are below with their versions. 
* Solidity - 0.5.16
* Web3.js - 1.2.9
* Truffle - 5.2.2
* Drizzle - 1.0.0
* Ganache - 2.5.4
* Metamask - 9.3.0
* Node - 10.19.0
* npm - 6.14.4

To help illustrate, I drew up the achitecture to provide a standard view of a decentralized application. You can see the user interacts with the React frontend and encounters the Web3.js api and check for Metamask. They then are able to interact with the Ethereum network which consists of many nodes, all containing smart contracts, storage options, and the EVM for processing. 

![dapp-architecture-img](assets/img/architecture.PNG)

This dapp structure begins with the frontend, which handles receiving the user input and sending to the backend i.e. smart contract. This occurs through the web.js api, with the help of Drizzle which uses React's context and state management to keep the frontend synced with the smart contracts data that it connects to. On the receiving end of the frontend's requests is the main `EventManager.sol` contract. This can be thought of as the parent that manages the child contracts, and exposes getter functions so the frontend can access data on any existing contracts that have been deployed. This contract can create new contracts by sending a request to the `EventCreator.sol` which has the sole job of creating and deploying event contracts for the EventManager contract to manage. The three smart contract event types are `PollEvent.sol`, `EscrowEvent.sol`, and `LotteryEvent.sol`. They each utilize a constructor that handles the initial configuration of the events logic. For exampe, candidate names for polls, involved parties for escrows, and ticket price for lotteries are all mapped to the contract's storage during this constructor process. At this point, the event contract has been configured and contains all the functions and logic needed to process that type of event. Below we will look at the struct in each contract event to better understand their makeup:

```

/**                       /**                            /**      
Poll Candidate            Escrow Member                  Lottery Ticket       
*/                        */                             */    
struct Candidate {        struct Member {                struct Ticket {                                                               
   uint id;                  uint id;                       uint id;
   string name;              address memberAddress;         address owner;                   
   uint voteCount;           uint currentDeposit;           uint number;
}                            uint requiredDeposit;          string status;
                           }                                bool winner;
                                                         } 
```

The `PollEvent.sol` contract maps each Candidate struct to their ID, and includes their name and total votes the candidates received. Poll events additionally map each address that votes in the poll so we can prevent any user from voting twice. Other functions for adding candidates, casting a vote, and getting data about the Poll are exposed so the frontend can grab what it needs and provide data to update the smart contract state. `EscrowEvent.sol` takes a similiar approach in how it stores each member involed in the escrow, mapping it to their ID and storing their address. Additionally the required amount the member must deposit, and the current amount they actually have deposited are stored and updated as the members make their deposits. The stored members address is used to validate that the frontend user attempting to deposit matches the address that was assigned to participate in the escrow. Finally, the `LotteryEvent.sol` contract follows the same pattern, mapping each created Ticket to its ID. A unique number is generated during construction and is assigned to each ticket, along with the status of if the ticket has been purchased or not, and if the ticket is the winner. This pattern of structing a data model, mapping it to storage, and updating its values through setters encompasses the basic pattern of a decentralized application.  

When discussing the logic used in a decentralized application, and in particular in smart contracts, we have to remember that each time we write to the blockchain, it costs the user money. Each additional loop we must make, each additional variable that needs incremented, and any unnecessary code all falls back onto the user in additional transaction feed. While developing this dapp, I had to constantly keep this in mind as the transaction data I would be gathering for my analysis could be heavily impacted by the computation my smart contracts required. 

Other challenges arose during development, and they stem from the fact that Solidity is a deterministic language. This is because each node needs to be able to get the same exact results with the given input for a method. Without this, it would not be possible to keep consensus within the network. Determinisitic languages means generating a random number isn't possible without work arounds or 3rd party solutions. I ended up using the `keccak256` hash function and the current block timestamp to create a pseudorandom number. The blockchain also has no concept of time, so I had to resort to using `uint` data types to store the block timestamp, which is the same as the Unix timestamp. Because of the difficulty of evaluting passing time, the expiration date for each contract event type may be off by up to a couple hours. The final hurdle is the fact that anything on a smart contract can be read by the user. This meant that the lottery contract which contains the information regarding the winning ticket could be viewed by a user if they really wanted to find it. The solution to this would require a third-party oracle, which is a service that can provide external information to a smart contract.

## User Experience & Interface
The main priority of the interface and experience of my dapp was simplicity. One aspect of the practicality of dapps is the ease of use for users, specifially new users. The functionality of the contract can be top-notch, however a poor UI and UX would negate that if the user can't even figure out how to use the contract. The first step in using any application on a decentralized network is to fund an account so you can complete transactions. For many new users, this is the most difficult step. There unfortunately isn't an easy solution such as "pre-loading" accounts for users, instead the best that can be done is often holding the user's hand and guiding them to trusted exchanges like Coinbase or Binance where they can purchase Cryptocurrencies like Ether. When demonstrating this dapp to friends and family, I often received confused looks when explaning that the user first needs to fund their account with Ether, the gas of the Ethereum network. After funding, the user needs to send their Ethereum to a browser wallet, since the dapp interface is browser-based. There are several browser wallets, and Metamask is the most popular. The extension can be downloaded in Google Chrome where a user can create an account to send their Ether to. At this point the user has Ether in their Metamask wallet, they're now able to interact with the dapp. From this point, the user will probably feel as if they're on a normal website, as the transactions occur seamlessly as they deploy their own events and interact with existing ones. 

I wanted to include some sort of branding for the dapp to better showcase the UI and give the users something unique to remember the it by. I went with the name Crêpe (many Ethereum projects use desert and food in their branding) and added a fun pink theme to the frontend. User interaction with input fields displays responsive styling, and the UI re-renders thanks to Drizzle whenever information in the contract is updated. Overall, the UI and UX for this dapp accomplished my goal of simplicity and provides the user all the information they need to learn about Ethereum, purchase it, and use it with Metamask on the Help page within the menu. Additionally, an About page is included that contains information about my project and its purpose. Below is an initial mockup of the UI and UX flow. I made some slight changes as I developed, but stayed true to most of the mockup. 

![dapp-interface-img](assets/img/interface.PNG?raw=true)

### Experience with Event Contracts:
Each event contract had unique logic needed to ensure they functioned properly. Additional layers of complexity could be added to make each event more customizable, however in keeping with my goal of simplicity, I included only what was necessary to make each event contract function in it's most basic form. As discussed above, too much complexity and you risk raising the amount of gas the user must pay for transactions. Once deployed, each event can be found on the Browse page, or by searching for the event's contract address on the Search page. Each contract type and their details are described below:

* Poll Event Contract: Poll events allow a user to create there own voting process contained in a single contract deployment. The authoring user names the poll, sets an expiration date for the poll, and can add candidates that can be voted for. When viewing a deployed poll, any other user can spend ethereum to cast their single vote. After the contract expires, no more votes can be cast and the winner can be viewed. This event type was most simple, and likely will require the least amount of gas to deploy and interact with. 

* Escrow Event Contract: Escrow events allow a user to create a escrow process contained in a single contract deployment. The authoring user names the escrow, sets the expiration, sets the required amount that each member of the escrow must deposit, and can add the address of any other user that they want to be part of the escrow. When viewing a deployed escrow, only the users who have designated addresses assigned to the escrow may interact with it. The designated users can deposit the required amount of Ether which the contract will lock up until the contract expires. At that point, the users will then be able to withdraw their deposited amount back to their own address from the contract. This event type was slighly more complex than the poll event, thus will probably use more gas as well.  

* Lottery Event Contract: Lottery events allow a user to create a lottery process contained in a single contract deployment. The authoring user names the lottery, sets the expiration, sets the total number of tickets to be generated in the lottery, and sets the cost to purchase a ticket. When viewing a deployed lottery, any other user can purchase as many tickets as they'd like. When purchasing a ticket the user will have their address assigned to it, and will transfer the cost of the ticket to the smart contract to lock up until expiration. Once expired, users can view the lottery and attempt to claim there ticket to see if it was the winner. Claiming a winning ticket will result in the contract sending the entire balance it received from the purchased tickets to the that users address. Certainly more complex than the poll and escrow event, this will likely use the most gas to deploy and interact with. 

## Evaluation Metric
The Ethereum network uses it's native token, Ether, as gas. This gas is the fee that is spent during each transaction, and currently goes to the miners of the network as a reward for validating blocks and keeping the chain in consensus. Gas is usually expressed in small fractions of Ether, and is commonly referred to by the unit "gwei". The price of Ether can be volatile, meaning gas prices are always in flux. High network congestion also impacts gas prices, as the miners must price the gas in accordance with supply and demand on the network. To evaluate the transactions of this dapp, we will be expressing costs in terms of gwei and the United States Dollar (USD). We will evaluating how transactions costs differ amongst transaction type, how costs differ for the same transaction over a period of time, and how the price of Ether impacts transaction costs over a period of time. 

## Data Insights
To analyze the costs of using the dapp, I needed to gather data. The two main components to look at were deployment costs and transaction costs. For each deployment and transaction that could occur in my dapp, I executed it ten times in a span of 10 minutes. During each execution, I logged information about the transaction into a csv file. This would allow me to easily pull in the data to Jupyter Notebook for analysis later on. Truffle and Ganache provided all the information I needed about each deployment and transaction, including the gas price, gas usage, and timestamps. I wanted to include the current price of Ether at the time of execution so I could see if it impacted costs at all. I ended up having to manually log the current price using the etherscan website as reference, which allowed me to take a snapshot of Ether prices so I could accurately record them into my csv files. Once I had all the data, I used pandas and matplotlib to create visualization of the data I had for each transaction. This allowed me easily compare different transactions to the price of gas at the time of execution, and compare how different contracts varied in cost. 

### EventManager & EventCreator Deployment
![EventManager Deploy](assets/img/EventManagerAndCreatorDeploy.png?raw=true)

The above image displays the deployment costs of the EventManager and EventCreator contract. It additionally shows the price of gas in gwei during the execution period. Immediately we can see that the EventCreator contract was far more expensive to deploy with the cost ranging between 0.65 ETH and 0.82 ETH. At the time of writing, one Ether costs $2,294.56. This means the EventCreator contract would cost the dapp deployer anywhere from $1,491.46 to $1,881.54 to deploy it. This deployment only needs to occur once on Ethereum's mainnet, however such a high cost would leave deploying such a contract to only those with a vested interest. Certinaly not practical for an everyday user. The expectations for these two contracts and their deployment costs was that the EventManager contract would be more expensive, as it contained many more lines of code and functionality. However we can see this isn't the case, and it may be because the EventCreator contract requires importing the Poll, Escrow, and Lottery contracts so it can create them later on. Importing another contract means extra computational power needed, as information about the contract has to be retreived such as it's application binary interface (abi). This is likely what caused the increased cost during deployment, as we can see even with gas prices at their lowest during this period, the costs were still nearly 3 times higher than the EventManager contract's costs.

### Poll, Escrow, & Lottery Contract Deployment
![PollEvent Deploy](assets/img/PollEventDeploy.png?raw=true)
![EscrowEvent Deploy](assets/img/EscrowEventDeploy.png?raw=true)
![LotteryEvent Deploy](assets/img/LotteryEventDeploy.png?raw=true)
![Compare Event Deploy](assets/img/EventContractsCompare.png?raw=true)

Above displays the respective costs and gas prices of deploying each of the contract event types. These deployments are initiated by the user from the dapp's frontend interface, and so the user must pay the deployment fees unlike with our EventManager and EventCreator contracts. We can see in the final chart that the Lottery event is much more costly than the Poll and Escrow events. It's nearly as expensive to deploy as the creator contract above, averaging around 0.63 ETH or $1,445.57 to deploy a single instance of a Lottery event in my dapp. It would be outrageous to expect an everyday user to pay this amount to try to hold a lottery event. The Lottery contract has much more logic than the Escrow and Poll contracts, however within each contract some looping has to occur. The Poll and Escrow contracts may have a few elements in an array to loop over, think 2 candidates to vote between, or a few parties involved in an escrow. However, the Lottery contract could contain a large amount of tickets, and in my execution had 15 tickets generated for each deployment. This meant many more loops compared to the looping that the Poll and Escrow contract had to do. As explained ealier, more computational power needed means higher costs, and this is likely the reason why our Lottery contract deployment costed so much. The Poll and Escrow clocked in with an average price around 0.25 ETH, or $573.64. Although much lower than the Lottery contract deployment, this cost still would assuredly scare away a new user to decentalized applications and smart contracts. 

### Event Contract Transactions
![Lottery Deploy Increasing Tickets](assets/img/LotteryIncreaseTicketDeploy.png?raw=true)
![Lottery Deploy Increasing Tickets Gas](assets/img/LotteryIncreaseTicketGas.png?raw=true)

One thing to consider is how deployment costs are impacted by the inputs the user provides. Looking at the lottery contract deployment, we can see that having many tickets can lead to higher costs. But now we can see how the cost changes as we increase the amount of tickets that need generated. The first image shows that as the number of tickets in the lottery increases, the price of deploying that contract increases as well. To confirm this correlation, we need to also see how gas prices might have impacted it. The second image shows the same ticket count plot, but now shows the gas price, which actually dropped midway through the executions. With the gas price dropping and the contract deployment costs still steadily rising in unison with the ticket count, we can safely conclude there's a positive correlation between the inputs and deployment costs. 

![Escrow Deposit Transaction](assets/img/EscrowDepositTransaction.png?raw=true)
![Poll Vote Transaction](assets/img/PollVoteTransaction.png?raw=true)

After examing the deployment costs of the various contracts, let's now look at the costs of a user interacting with a deployed contract. One example we can look at is a user making their required deposit into an Escrow smart contract. You can see this has a much lower fee than what we've already seen, around 0.0065 ETh on average. This would be $14.91, and would definitely be more appropriate of a price for an everyday user. Keep in mind this deposit requires the transfering of Ether from the user's address to the smart contract, and still has a reasonably low cost. We can compare this transfering to the cost of a user casting a single vote in the Poll event contract. Casting a vote is essentially executing a write to the smart contract, incrementing the selected candidates voteCount variable by one. Some additional checks are required, and we can see it has a slightly higher cost than the escrow deposit. At 0.0125 on average, or $28.68, a user may hesitate to cast their vote, however still much more reasonable than some of the other costs we've seen. 

### Ether and Gwei Correlation
![Eth Gwei Correlation 1](assets/img/eth-gwei-1.png?raw=true)
![Eth Gwei Correlation 2](assets/img/eth-gwei-2.png?raw=true)
![Eth Gwei Correlation 3](assets/img/eth-gwei-3.png?raw=true)

While evaluating this data, it's important to keep in mind how Ether and gas prices fluctuate. Without tracking their prices, one could easily misinterpret why a transaction cost is high or low. In the images above we have three seperate time periods where Ether and gas prices were tracked. This time periods were relatively short, but show how a subtle move in Ether's price can cause a volatile reacion in the price of gas. Ether's price has increased from $730.11 in the start of January to $2,294.56 currently, and gas prices have increased along with it. One year ago the average gwei per transaction was 10 gwei. Today it averages about 109 gwei, about a 1000% increase in gas prices.

## Analysis 
The collected data and analysis provided the insight needed to address the stated research questions. The study found the expected transaction costs that a user could experience while interacting with a dapp. These costs were quite high when compared to other alternative options, with the user spending hundreds or even thousands to deploy their own smart contract. While we looked at these costs, we explored the factors that influence how much gas needs to be used in each transaction. The amount of user input causes higher costs, which we saw when analzying the effects of having to generate additional tickets for the lottery contract deployment. Furthermore, we observed Ether's price fluctuations and how they directly correlate to the price of gas on the network. This inturn influences how much users on the network have to pay to interact with the blockchain. Even with proper architecture and engineering, it can be difficult to keep costs low for users when designing and developing a decentralized application. If the price of Ether continues to rise, one can assume gas prices will rise with it, and it becomes unclear of how users will interact with a blockchain that has such high fees. Scaling solutions are said to be on the way for Ethereum, but it's yet to be seen if they will be effective in reducing the cost for users. In it's current state, Ethereum functions properly and can do some unique and powerful things that may benefit our society, but the transaction costs are far too high to expect a regular person to use them. 

## Conclusion
Blockchain technology's growth has brought many to question it's practicality in our everyday lives. Decentralized applications and smart contracts are being developed for a variety of usecases, and in every different field. Ethereum in particular is one of the most used blockchains for smart contracts, and our study demonstrates how to develop a dapp on Ethereum and explore the costs associated with it. Using python and some data science tools, We identified the average costs a user could expect based on transaction types, and explored what factors, such as Ether's gas price, could impact it. Overall, this study demonstrated that decentralized applications may not yet be ready to handle events effectively, such as a poll or lottery, for everyday users who are unfamiliar with decentralized technology. It can certainly handle processing events that require trust, however the question becomes are users willing to pay for the benefits of decentralization, and at what cost? This question will likely be answered in the coming years as blockchain technology matures and improves its scalability. 